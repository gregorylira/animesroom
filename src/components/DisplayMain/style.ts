import styled from "styled-components";

export const Main = styled.main`
  width: 100%;
  height: 10rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  color: var(--text);
  font-size: 2rem;
`;
